- name: Demo on conditions
  hosts: localhost
  vars: 
    OS: Centos
  tasks:
    - name: Centos
      debug:
        msg: Centos
      when: OS == "Centos"
      
    - name: Ubuntu
      debug:
        msg: Ubuntu
      when: OS == "ubuntu"
        